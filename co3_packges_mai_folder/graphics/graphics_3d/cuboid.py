def surface_area(length, breadth, height):
    return 2* ((length * breadth) + (length * height) + (breadth * height))

def edge_perimeter (length, breadth, height):
    return 4 * (length + breadth + height)